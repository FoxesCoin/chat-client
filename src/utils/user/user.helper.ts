import { COLORS } from 'styles/color';

// TODO add for all statuses different color

export const STATUS_COLOR: Record<UserStatus, string> = {
  active: COLORS.green,
  away: COLORS.green,
  disabled: COLORS.green,
};
